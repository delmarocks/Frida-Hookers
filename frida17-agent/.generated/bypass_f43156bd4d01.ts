import Java from "frida-java-bridge";

(globalThis as any).Java = Java;

function evalLegacy(source: string, label: string): void {
  (0, eval)(`${source}\n//# sourceURL=${label}`);
}

evalLegacy("\n(function () {\n    function toText(args) {\n        try {\n            return Array.prototype.slice.call(args).map(function (item) {\n                if (typeof item === \"string\") {\n                    return item;\n                }\n                try {\n                    return JSON.stringify(item);\n                } catch (_) {\n                    return String(item);\n                }\n            }).join(\" \");\n        } catch (_) {\n            return \"[console bridge serialize failed]\";\n        }\n    }\n\n    function bridge(level, originalFn) {\n        return function () {\n            var text = toText(arguments);\n            try {\n                send({\n                    type: \"console\",\n                    level: level,\n                    message: text\n                });\n            } catch (_) {\n            }\n            if (typeof originalFn === \"function\") {\n                try {\n                    originalFn.apply(console, arguments);\n                } catch (_) {\n                }\n            }\n        };\n    }\n\n    if (typeof console !== \"undefined\") {\n        console.log = bridge(\"log\", console.log);\n        console.warn = bridge(\"warn\", console.warn);\n        console.error = bridge(\"error\", console.error);\n    }\n})();\n", 'console-bridge.js');
evalLegacy("'use strict';\n\n// ============================================================\n// \u914d\u7f6e\u533a\n// ============================================================\n\n// \u76ee\u6807 so \u540d\u79f0\nconst ARMOR_SO = 'libsi_armor.so';\nconst CRASH_SO = 'libsi_crashsdk.so';\n\n// \u5305\u540d\uff0c\u4ec5\u7528\u4e8e\u65e5\u5fd7\u663e\u793a\nconst TARGET_PKG = 'com.zzkko';\n\n// \u65e5\u5fd7\u5f00\u5173\nconst LOG_INFO = true;\nconst LOG_WARN = true;\nconst LOG_ERROR = true;\nconst LOG_DEBUG = false;\n\n// \u529f\u80fd\u5f00\u5173\nconst ENABLE_ARMOR_PATCH = true;      // \u662f\u5426 hook libsi_armor.so \u5173\u952e\u68c0\u6d4b\u70b9\nconst ENABLE_CRASH_PATCH = true;      // \u662f\u5426 hook libsi_crashsdk.so \u5173\u952e\u68c0\u6d4b\u70b9\nconst ENABLE_LIBC_HIDE = false;       // \u662f\u5426 hook libc \u505a\u5168\u5c40\u9690\u85cf\nconst ENABLE_JAVA_BYPASS = true;      // \u662f\u5426 hook Java \u5c42 RootDetector / NativeInterface\nconst ENABLE_STRSTR_HIDE = true;      // \u662f\u5426\u62e6\u622a strstr\nconst ENABLE_OPEN_HIDE = true;        // \u662f\u5426\u62e6\u622a open/openat/fopen/access/opendir\nconst ENABLE_DLOPEN_WATCH = true;     // \u662f\u5426\u76d1\u63a7 dlopen\uff0c\u9002\u5408 spawn \u573a\u666f\nconst ENABLE_EXIT_GUARD = false;      // \u662f\u5426\u62e6\u622a abort/exit/kill/raise \u7b49\u9000\u51fa\u94fe\nconst ENABLE_EXIT_TRACE = true;       // \u662f\u5426\u6253\u5370\u9000\u51fa\u94fe\u8c03\u7528\u6808\n\n// spawn \u573a\u666f\u4f18\u5316\uff1a\u6a21\u5757\u8f6e\u8be2\u7b49\u5f85\u65f6\u95f4\nconst MODULE_WAIT_INTERVAL = 200;\nconst MODULE_WAIT_MAX_RETRY = 300;    // 300 * 200ms = 60 \u79d2\n\n// \u9700\u8981\u9690\u85cf\u7684\u8def\u5f84\nconst HIDE_PATHS = [\n    '/proc/self/maps',\n    '/proc/self/task',\n    '/proc/self/status',\n    '/data/local/tmp/frida_server'\n];\n\n// \u9700\u8981\u9690\u85cf\u7684\u5173\u952e\u5b57\nconst HIDE_KEYWORDS = [\n    'frida',\n    'frida-agent',\n    'frida-gadget',\n    'gum-js',\n    'gmain',\n    'gdbus',\n    'xposed',\n    'substrate',\n    'magisk'\n];\n\n// \u5df2\u786e\u8ba4\u504f\u79fb\nconst OFFSETS = {\n    armor: {\n        mapsScan: 0x67F50,      // sub_67F50\uff0c\u626b\u63cf /proc/self/maps \u4e2d\u7684 frida/xposed/substrate\n        threadScan: 0x8339C,    // sub_8339C\uff0c\u626b\u63cf\u7ebf\u7a0b\u540d gum-js/gmain/gdbus/frida\n        rwxScan: 0x849F8,       // sub_849F8\uff0c\u626b\u63cf\u5f02\u5e38 rwx / \u53ef\u6267\u884c\u9875\n        mapsRule1: 0x7F2DC,     // sub_7F2DC\uff0cmaps \u89c4\u5219\u5f15\u64ce\n        mapsRule2: 0x79F0C      // sub_79F0C\uff0c\u53e6\u4e00\u5957 maps \u89c4\u5219\u5f15\u64ce\n    },\n    crash: {\n        nativeCheckRooted: 0x4BE8, // sub_4BE8\uff0c\u5bf9\u5e94 nativeCheckRooted()\n        rootCheckCore: 0x8090      // sub_8090\uff0croot/frida_server \u6587\u4ef6\u68c0\u6d4b\u6838\u5fc3\n    }\n};\n\n// \u9632\u91cd\u590d hook \u6807\u8bb0\nconst HOOK_STATE = {\n    armor: false,\n    crash: false,\n    libc: false,\n    java: false,\n    exitGuard: false\n};\n\n// ============================================================\n// \u65e5\u5fd7\n// ============================================================\n\nfunction now() {\n    return new Date().toISOString();\n}\n\nfunction info(msg) {\n    if (LOG_INFO) console.log('[INFO][' + now() + '] ' + msg);\n}\n\nfunction warn(msg) {\n    if (LOG_WARN) console.log('[WARN][' + now() + '] ' + msg);\n}\n\nfunction error(msg) {\n    if (LOG_ERROR) console.log('[ERR ][' + now() + '] ' + msg);\n}\n\nfunction debug(msg) {\n    if (LOG_DEBUG) console.log('[DBG ][' + now() + '] ' + msg);\n}\n\nfunction trace(msg) {\n    console.log('[TRACE][' + now() + '] ' + msg);\n}\n\n// ============================================================\n// \u517c\u5bb9\u8f85\u52a9\n// ============================================================\n\n// \u5b89\u5168\u8bfb\u5b57\u7b26\u4e32\uff0c\u907f\u514d Memory.readCString \u5d29\nfunction safeReadCString(p) {\n    try {\n        if (!p || p.isNull()) return '';\n        return Memory.readCString(p);\n    } catch (e) {\n        return '';\n    }\n}\n\n// \u5224\u65ad\u5b57\u7b26\u4e32\u4e2d\u662f\u5426\u5305\u542b\u9700\u8981\u9690\u85cf\u7684\u5bf9\u8c61\nfunction containsHidden(s) {\n    if (!s) return false;\n    const lower = String(s).toLowerCase();\n\n    for (let i = 0; i < HIDE_PATHS.length; i++) {\n        if (lower.indexOf(HIDE_PATHS[i].toLowerCase()) !== -1) return true;\n    }\n\n    for (let i = 0; i < HIDE_KEYWORDS.length; i++) {\n        if (lower.indexOf(HIDE_KEYWORDS[i].toLowerCase()) !== -1) return true;\n    }\n\n    return false;\n}\n\n// \u5b89\u5168\u6253\u5370 Native \u8c03\u7528\u6808\nfunction logNativeBacktrace(context, tag) {\n    if (!ENABLE_EXIT_TRACE) return;\n    try {\n        const lines = Thread.backtrace(context, Backtracer.ACCURATE)\n            .map(function (addr) {\n                const sym = DebugSymbol.fromAddress(addr);\n                return '    ' + addr + ' => ' + sym;\n            })\n            .join('\\n');\n        trace(tag + '\\n' + lines);\n    } catch (e) {\n        error('logNativeBacktrace failed: ' + e);\n    }\n}\n\n// \u6ce8\u518c Native \u5f02\u5e38\u5904\u7406\uff0c\u4fbf\u4e8e\u533a\u5206\u662f\u4e3b\u52a8\u9000\u51fa\u8fd8\u662f\u5d29\u6e83\nfunction installExceptionHandler() {\n    try {\n        if (typeof Process.setExceptionHandler !== 'function') {\n            warn('Process.setExceptionHandler not available');\n            return;\n        }\n\n        Process.setExceptionHandler(function (details) {\n            try {\n                error('[exception] type=' + details.type +\n                    ', address=' + details.address +\n                    ', memory=' + details.memory);\n\n                if (details.context) {\n                    logNativeBacktrace(details.context, 'native exception backtrace');\n                }\n            } catch (e) {\n                error('exception handler logging failed: ' + e);\n            }\n\n            return false;\n        });\n\n        info('[+] native exception handler installed');\n    } catch (e) {\n        error('installExceptionHandler failed: ' + e);\n    }\n}\n\n// \u517c\u5bb9\u83b7\u53d6\u6a21\u5757\u5bf9\u8c61\uff0c\u9002\u914d Frida 17.x\nfunction findModuleCompat(name) {\n    try {\n        if (typeof Process.findModuleByName === 'function') {\n            return Process.findModuleByName(name);\n        }\n    } catch (e) {}\n\n    try {\n        if (typeof Process.getModuleByName === 'function') {\n            return Process.getModuleByName(name);\n        }\n    } catch (e) {}\n\n    return null;\n}\n\n// \u517c\u5bb9\u83b7\u53d6\u6a21\u5757\u57fa\u5740\nfunction getModuleBase(name) {\n    try {\n        const m = findModuleCompat(name);\n        if (m && m.base) return m.base;\n    } catch (e) {}\n    return null;\n}\n\n// \u517c\u5bb9\u67e5\u627e\u5bfc\u51fa\uff0c\u9002\u914d Frida 17.x\nfunction findExportCompat(moduleName, exportName) {\n    try {\n        if (typeof Module.getExportByName === 'function') {\n            return Module.getExportByName(moduleName, exportName);\n        }\n    } catch (e) {}\n\n    try {\n        if (typeof Module.findExportByName === 'function') {\n            return Module.findExportByName(moduleName, exportName);\n        }\n    } catch (e) {}\n\n    return null;\n}\n\n// \u901a\u7528 attach \u5305\u88c5\nfunction safeAttach(addr, callbacks, tag) {\n    try {\n        Interceptor.attach(addr, callbacks);\n        info('[+] attached: ' + tag + ' @ ' + addr);\n        return true;\n    } catch (e) {\n        error('attach failed: ' + tag + ', err=' + e);\n        return false;\n    }\n}\n\n// \u901a\u7528 replace \u5305\u88c5\nfunction safeReplace(addr, callback, tag) {\n    try {\n        Interceptor.replace(addr, callback);\n        info('[+] replaced: ' + tag + ' @ ' + addr);\n        return true;\n    } catch (e) {\n        error('replace failed: ' + tag + ', err=' + e);\n        return false;\n    }\n}\n\n// \u6309 module base + offset \u83b7\u53d6\u76ee\u6807\u5730\u5740\nfunction resolveAddress(moduleName, offset) {\n    const base = getModuleBase(moduleName);\n    if (!base) return null;\n    return base.add(offset);\n}\n\n// ============================================================\n// Native hook\uff1a\u7edf\u4e00\u8fd4\u56de\u503c\u7be1\u6539\n// ============================================================\n\n// \u628a\u51fd\u6570\u8fd4\u56de\u503c\u6539\u6210 0\nfunction hookRet0(moduleName, offset, tag) {\n    const target = resolveAddress(moduleName, offset);\n    if (!target) {\n        warn('module not loaded, skip ' + tag);\n        return false;\n    }\n\n    return safeAttach(target, {\n        onEnter: function () {\n            debug('[>] enter ' + tag);\n        },\n        onLeave: function (retval) {\n            try {\n                retval.replace(ptr(0));\n                info('[PATCH] ' + tag + ' => 0');\n            } catch (e) {\n                error('patch retval failed: ' + tag + ', err=' + e);\n            }\n        }\n    }, tag);\n}\n\n// \u628a\u51fd\u6570\u8fd4\u56de\u503c\u6539\u6210 NULL\nfunction hookRetNull(moduleName, offset, tag) {\n    const target = resolveAddress(moduleName, offset);\n    if (!target) {\n        warn('module not loaded, skip ' + tag);\n        return false;\n    }\n\n    return safeAttach(target, {\n        onEnter: function () {\n            debug('[>] enter ' + tag);\n        },\n        onLeave: function (retval) {\n            try {\n                retval.replace(ptr(0));\n                info('[PATCH] ' + tag + ' => NULL');\n            } catch (e) {\n                error('patch retval failed: ' + tag + ', err=' + e);\n            }\n        }\n    }, tag);\n}\n\n// ============================================================\n// hook libsi_armor.so\n// ============================================================\n\nfunction hookArmor() {\n    if (!ENABLE_ARMOR_PATCH) {\n        warn('ENABLE_ARMOR_PATCH = false, skip armor hooks');\n        return;\n    }\n\n    if (HOOK_STATE.armor) {\n        debug('armor already hooked');\n        return;\n    }\n\n    info('[*] try hook libsi_armor.so');\n\n    // \u7ebf\u7a0b\u626b\u63cf\uff1agum-js / gmain / gdbus / frida\n    hookRetNull(ARMOR_SO, OFFSETS.armor.threadScan, 'armor.threadScan(sub_8339C)');\n\n    // maps \u626b\u63cf\uff1afrida-agent / frida-gadget / xposed / substrate\n    hookRetNull(ARMOR_SO, OFFSETS.armor.mapsScan, 'armor.mapsScan(sub_67F50)');\n\n    // \u5f02\u5e38 rwx \u9875\u68c0\u6d4b\n    hookRet0(ARMOR_SO, OFFSETS.armor.rwxScan, 'armor.rwxScan(sub_849F8)');\n\n    // maps \u89c4\u5219\u5f15\u64ce 1\n    hookRet0(ARMOR_SO, OFFSETS.armor.mapsRule1, 'armor.mapsRule1(sub_7F2DC)');\n\n    // maps \u89c4\u5219\u5f15\u64ce 2\n    hookRet0(ARMOR_SO, OFFSETS.armor.mapsRule2, 'armor.mapsRule2(sub_79F0C)');\n\n    HOOK_STATE.armor = true;\n    info('[+] armor hooks done');\n}\n\n// ============================================================\n// hook libsi_crashsdk.so\n// ============================================================\n\nfunction hookCrash() {\n    if (!ENABLE_CRASH_PATCH) {\n        warn('ENABLE_CRASH_PATCH = false, skip crash hooks');\n        return;\n    }\n\n    if (HOOK_STATE.crash) {\n        debug('crash already hooked');\n        return;\n    }\n\n    info('[*] try hook libsi_crashsdk.so');\n\n    // JNI \u5bfc\u51fa\u7684 nativeCheckRooted\n    hookRet0(CRASH_SO, OFFSETS.crash.nativeCheckRooted, 'crash.nativeCheckRooted(sub_4BE8)');\n\n    // \u771f\u6b63\u7684 root/frida_server \u68c0\u6d4b\u6838\u5fc3\n    hookRet0(CRASH_SO, OFFSETS.crash.rootCheckCore, 'crash.rootCheckCore(sub_8090)');\n\n    HOOK_STATE.crash = true;\n    info('[+] crash hooks done');\n}\n\n// ============================================================\n// hook libc\uff1a\u5168\u5c40\u515c\u5e95\u9690\u85cf\n// ============================================================\n\nfunction hookLibcOpenLike() {\n    if (!ENABLE_OPEN_HIDE) {\n        warn('ENABLE_OPEN_HIDE = false, skip open/openat/fopen/access/opendir');\n        return;\n    }\n\n    const fopenPtr = findExportCompat(null, 'fopen');\n    const openPtr = findExportCompat(null, 'open');\n    const openatPtr = findExportCompat(null, 'openat');\n    const accessPtr = findExportCompat(null, 'access');\n    const opendirPtr = findExportCompat(null, 'opendir');\n\n    if (fopenPtr) {\n        safeAttach(fopenPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n                this.block = containsHidden(this.path);\n                if (this.block) warn('[libc] fopen blocked: ' + this.path);\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.block) retval.replace(ptr(0));\n                } catch (e) {\n                    error('fopen patch failed: ' + e);\n                }\n            }\n        }, 'libc.fopen');\n    }\n\n    if (openPtr) {\n        safeAttach(openPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n                this.block = containsHidden(this.path);\n                if (this.block) warn('[libc] open blocked: ' + this.path);\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.block) retval.replace(ptr(-1));\n                } catch (e) {\n                    error('open patch failed: ' + e);\n                }\n            }\n        }, 'libc.open');\n    }\n\n    if (openatPtr) {\n        safeAttach(openatPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[1]);\n                this.block = containsHidden(this.path);\n                if (this.block) warn('[libc] openat blocked: ' + this.path);\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.block) retval.replace(ptr(-1));\n                } catch (e) {\n                    error('openat patch failed: ' + e);\n                }\n            }\n        }, 'libc.openat');\n    }\n\n    if (accessPtr) {\n        safeAttach(accessPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n                this.block = containsHidden(this.path);\n                if (this.block) warn('[libc] access blocked: ' + this.path);\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.block) retval.replace(ptr(-1));\n                } catch (e) {\n                    error('access patch failed: ' + e);\n                }\n            }\n        }, 'libc.access');\n    }\n\n    if (opendirPtr) {\n        safeAttach(opendirPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n                this.block = containsHidden(this.path);\n                if (this.block) warn('[libc] opendir blocked: ' + this.path);\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.block) retval.replace(ptr(0));\n                } catch (e) {\n                    error('opendir patch failed: ' + e);\n                }\n            }\n        }, 'libc.opendir');\n    }\n}\n\nfunction hookLibcStrstr() {\n    if (!ENABLE_STRSTR_HIDE) {\n        warn('ENABLE_STRSTR_HIDE = false, skip strstr');\n        return;\n    }\n\n    const strstrPtr = findExportCompat(null, 'strstr');\n    if (!strstrPtr) {\n        warn('strstr not found');\n        return;\n    }\n\n    safeAttach(strstrPtr, {\n        onEnter: function (args) {\n            this.haystack = safeReadCString(args[0]);\n            this.needle = safeReadCString(args[1]);\n            this.block = containsHidden(this.haystack) || containsHidden(this.needle);\n        },\n        onLeave: function (retval) {\n            try {\n                if (this.block) {\n                    warn('[libc] strstr blocked: \"' + this.needle + '\"');\n                    retval.replace(ptr(0));\n                }\n            } catch (e) {\n                error('strstr patch failed: ' + e);\n            }\n        }\n    }, 'libc.strstr');\n}\n\nfunction hookLibc() {\n    if (!ENABLE_LIBC_HIDE) {\n        warn('ENABLE_LIBC_HIDE = false, skip libc hooks');\n        return;\n    }\n\n    if (HOOK_STATE.libc) {\n        debug('libc already hooked');\n        return;\n    }\n\n    info('[*] try hook libc');\n\n    hookLibcOpenLike();\n    hookLibcStrstr();\n\n    HOOK_STATE.libc = true;\n    info('[+] libc hooks done');\n}\n\n// ============================================================\n// hook \u9000\u51fa\u94fe\uff1a\u62e6\u622a\u4e3b\u52a8\u81ea\u6740/\u5f02\u5e38\u7ed3\u675f\n// ============================================================\n\nfunction hookExitGuard() {\n    if (!ENABLE_EXIT_GUARD) {\n        warn('ENABLE_EXIT_GUARD = false, skip exit guard');\n        return;\n    }\n\n    if (HOOK_STATE.exitGuard) {\n        debug('exitGuard already hooked');\n        return;\n    }\n\n    info('[*] try hook exit guard');\n\n    const abortPtr = findExportCompat(null, 'abort');\n    const exitPtr = findExportCompat(null, 'exit');\n    const _exitPtr = findExportCompat(null, '_exit');\n    const raisePtr = findExportCompat(null, 'raise');\n    const killPtr = findExportCompat(null, 'kill');\n    const tgkillPtr = findExportCompat(null, 'tgkill');\n    const pthreadKillPtr = findExportCompat(null, 'pthread_kill');\n\n    if (abortPtr) {\n        safeReplace(abortPtr, new NativeCallback(function () {\n            warn('[exit-guard] abort() blocked');\n            logNativeBacktrace(this.context, 'abort() backtrace');\n        }, 'void', []), 'libc.abort');\n    }\n\n    if (exitPtr) {\n        safeReplace(exitPtr, new NativeCallback(function (code) {\n            warn('[exit-guard] exit(' + code + ') blocked');\n            logNativeBacktrace(this.context, 'exit() backtrace');\n        }, 'void', ['int']), 'libc.exit');\n    }\n\n    if (_exitPtr) {\n        safeReplace(_exitPtr, new NativeCallback(function (code) {\n            warn('[exit-guard] _exit(' + code + ') blocked');\n            logNativeBacktrace(this.context, '_exit() backtrace');\n        }, 'void', ['int']), 'libc._exit');\n    }\n\n    if (raisePtr) {\n        safeReplace(raisePtr, new NativeCallback(function (sig) {\n            warn('[exit-guard] raise(' + sig + ') blocked');\n            logNativeBacktrace(this.context, 'raise() backtrace');\n            return 0;\n        }, 'int', ['int']), 'libc.raise');\n    }\n\n    if (killPtr) {\n        safeReplace(killPtr, new NativeCallback(function (pid, sig) {\n            warn('[exit-guard] kill(' + pid + ', ' + sig + ') blocked');\n            logNativeBacktrace(this.context, 'kill() backtrace');\n            return 0;\n        }, 'int', ['int', 'int']), 'libc.kill');\n    }\n\n    if (tgkillPtr) {\n        safeReplace(tgkillPtr, new NativeCallback(function (tgid, tid, sig) {\n            warn('[exit-guard] tgkill(' + tgid + ', ' + tid + ', ' + sig + ') blocked');\n            logNativeBacktrace(this.context, 'tgkill() backtrace');\n            return 0;\n        }, 'int', ['int', 'int', 'int']), 'libc.tgkill');\n    }\n\n    if (pthreadKillPtr) {\n        safeReplace(pthreadKillPtr, new NativeCallback(function (thread, sig) {\n            warn('[exit-guard] pthread_kill(' + thread + ', ' + sig + ') blocked');\n            logNativeBacktrace(this.context, 'pthread_kill() backtrace');\n            return 0;\n        }, 'int', ['pointer', 'int']), 'libc.pthread_kill');\n    }\n\n    HOOK_STATE.exitGuard = true;\n    info('[+] exit guard hooks done');\n}\n\n// ============================================================\n// hook Java \u5c42\n// ============================================================\n\nfunction hookJava() {\n    if (!ENABLE_JAVA_BYPASS) {\n        warn('ENABLE_JAVA_BYPASS = false, skip java hooks');\n        return;\n    }\n\n    if (HOOK_STATE.java) {\n        debug('java already hooked');\n        return;\n    }\n\n    if (!Java.available) {\n        warn('Java not available');\n        return;\n    }\n\n    Java.perform(function () {\n        info('[*] try hook Java layer');\n\n        try {\n            const RootDetector = Java.use('com.shein.crash.sdk.RootDetector');\n            RootDetector.c.implementation = function () {\n                warn('[java] RootDetector.c() => false');\n                return false;\n            };\n            info('[+] hooked RootDetector.c');\n        } catch (e) {\n            error('hook RootDetector.c failed: ' + e);\n        }\n\n        try {\n            const NativeIf = Java.use('com.shein.crash.sdk.SiCrashNativeInterface');\n            NativeIf.a.implementation = function () {\n                warn('[java] SiCrashNativeInterface.a() => false');\n                return false;\n            };\n            info('[+] hooked SiCrashNativeInterface.a');\n        } catch (e) {\n            error('hook SiCrashNativeInterface.a failed: ' + e);\n        }\n\n        try {\n            const JProcess = Java.use('android.os.Process');\n            JProcess.killProcess.implementation = function (pid) {\n                warn('[java-exit] android.os.Process.killProcess(' + pid + ') blocked');\n                return;\n            };\n            info('[+] hooked android.os.Process.killProcess');\n        } catch (e) {\n            error('hook android.os.Process.killProcess failed: ' + e);\n        }\n\n        try {\n            const JSystem = Java.use('java.lang.System');\n            JSystem.exit.implementation = function (code) {\n                warn('[java-exit] java.lang.System.exit(' + code + ') blocked');\n                return;\n            };\n            info('[+] hooked java.lang.System.exit');\n        } catch (e) {\n            error('hook java.lang.System.exit failed: ' + e);\n        }\n\n        HOOK_STATE.java = true;\n        info('[+] java hooks done');\n    });\n}\n\n// ============================================================\n// \u6a21\u5757\u7b49\u5f85 / dlopen \u76d1\u542c\n// ============================================================\n\n// \u7b49\u5f85\u6307\u5b9a\u6a21\u5757\u52a0\u8f7d\u540e\u6267\u884c\u56de\u8c03\nfunction waitForModule(name, callback) {\n    let retry = 0;\n\n    const timer = setInterval(function () {\n        try {\n            const base = getModuleBase(name);\n            if (base) {\n                clearInterval(timer);\n                info('[+] module loaded: ' + name + ' @ ' + base);\n                callback(base);\n                return;\n            }\n\n            retry++;\n            if (retry >= MODULE_WAIT_MAX_RETRY) {\n                clearInterval(timer);\n                warn('waitForModule timeout: ' + name);\n            }\n        } catch (e) {\n            clearInterval(timer);\n            error('waitForModule failed: ' + name + ', err=' + e);\n        }\n    }, MODULE_WAIT_INTERVAL);\n}\n\n// \u76d1\u63a7 dlopen/android_dlopen_ext\uff0c\u9002\u914d spawn \u6a21\u5f0f\nfunction watchDlopen() {\n    if (!ENABLE_DLOPEN_WATCH) {\n        warn('ENABLE_DLOPEN_WATCH = false, skip dlopen watch');\n        return;\n    }\n\n    const dlopenPtr = findExportCompat(null, 'dlopen');\n    const androidDlopenExtPtr = findExportCompat(null, 'android_dlopen_ext');\n\n    if (dlopenPtr) {\n        safeAttach(dlopenPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n            },\n            onLeave: function () {\n                if (!this.path) return;\n\n                if (this.path.indexOf(ARMOR_SO) !== -1) {\n                    warn('[dlopen] loaded ' + ARMOR_SO);\n                    hookArmor();\n                }\n\n                if (this.path.indexOf(CRASH_SO) !== -1) {\n                    warn('[dlopen] loaded ' + CRASH_SO);\n                    hookCrash();\n                }\n            }\n        }, 'dlopen');\n    }\n\n    if (androidDlopenExtPtr) {\n        safeAttach(androidDlopenExtPtr, {\n            onEnter: function (args) {\n                this.path = safeReadCString(args[0]);\n            },\n            onLeave: function () {\n                if (!this.path) return;\n\n                if (this.path.indexOf(ARMOR_SO) !== -1) {\n                    warn('[android_dlopen_ext] loaded ' + ARMOR_SO);\n                    hookArmor();\n                }\n\n                if (this.path.indexOf(CRASH_SO) !== -1) {\n                    warn('[android_dlopen_ext] loaded ' + CRASH_SO);\n                    hookCrash();\n                }\n            }\n        }, 'android_dlopen_ext');\n    }\n}\n\n// ============================================================\n// \u542f\u52a8\u5165\u53e3\n// ============================================================\n\nfunction main() {\n    info('script start for ' + TARGET_PKG);\n\n    try { installExceptionHandler(); } catch (e) { error('installExceptionHandler failed: ' + e); }\n    try { hookLibc(); } catch (e) { error('hookLibc failed: ' + e); }\n    try { hookExitGuard(); } catch (e) { error('hookExitGuard failed: ' + e); }\n    try { watchDlopen(); } catch (e) { error('watchDlopen failed: ' + e); }\n\n    try {\n        if (Java.available) {\n            Java.perform(function () {\n                hookJava();\n            });\n        }\n    } catch (e) {\n        error('hookJava failed: ' + e);\n    }\n\n    try {\n        waitForModule(ARMOR_SO, function () {\n            hookArmor();\n        });\n    } catch (e) {\n        error('wait armor failed: ' + e);\n    }\n\n    try {\n        waitForModule(CRASH_SO, function () {\n            hookCrash();\n        });\n    } catch (e) {\n        error('wait crash failed: ' + e);\n    }\n\n    info('init done');\n}\n\nsetImmediate(main);\n", "bypass.js");
evalLegacy("rpc.exports = {\n    cleanup: function () {\n        // \u6e05\u7406\u6240\u6709\u62e6\u622a\u5668\n        Interceptor.detachAll();\n        // \u5982\u679c\u4f60\u8bbe\u7f6e\u4e86\u5b9a\u65f6\u5668\u6216 Stalker\uff0c\u4e5f\u53ef\u4ee5\u6e05\u7406\n        Stalker.unfollow();\n        // clearInterval(...);\n    }\n};", '_hook_js_warp.js');
